name: Import Dashboard

on:
  push:
    branches:
      - master

env:
  SUPERSET_USERNAME: admin
  SUPERSET_PASSWORD: admin
  DBS_PASSWORDS: {"ClickHouse Connect (Superset)": "4LJRJj8RSimRzM62"}

jobs:
  uses: actions/import-superset-dashboard-action@v1
  with:
    url_base: http://localhost:8088
    dashboard_file_path: Dashboards/dashboard_test.zip
    overwrite: true